<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/"><%= t("site.site_name") %></a>
            <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-ex1-collapse">
              <span class="sr-only"><%= t("site.navbar.nav") %></span>
               <span class="icon-bar"></span>
               <span class="icon-bar"></span>
               <span class="icon-bar"></span>
            </button>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">

              <% if !current_user %>
                <li><%= link_to( t("site.navbar.registration"), new_user_registration_path) %> </li>
                <li><%= link_to( t("site.navbar.new_session"), new_user_session_path) %></li>
              <% else %>
                <li>
                  <%= link_to conversations_path do %>
                    <i class="fa fa-envelope" aria-hidden="true"></i>
                  <% end %>
                </li>
                <li>
                  <%= link_to notifications_path do %>
                    <%= display_notification_count(notification_count) %>
                  <% end %>
                </li>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      <i class="fa fa-user-circle" aria-hidden="true"></i> <%= current_user.display_name %>
                      <b class="caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                      <% if current_user.admin? %>
                        <li><%= link_to( t("site.navbar.complain"), admin_complains_path) %></li>
                        <li><%= link_to( t("site.navbar.quiz"), admin_quizzes_path) %></li>
                        <li><%= link_to( t("site.navbar.question"), admin_questions_path) %></li>
                        <li><%= link_to( t("site.navbar.category"), admin_categories_path) %></li>
                        <li><%= link_to( t("site.navbar.type"), admin_types_path) %></li>
                        <li class="divider"></li>
                      <% end %>
                      <li> <%= display_favorite_link %> </li>
                      <li> <%= display_wrong_link %> </li>
                      <li class="divider"></li>
                      <li> <%= link_to( t("site.navbar.user"), edit_user_path) %> </li>
                      <li> <%= link_to( t("site.navbar.logout"), destroy_user_session_path, method: :delete) %> </li>
                  </ul>
                </li>
              <% end %>

            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
